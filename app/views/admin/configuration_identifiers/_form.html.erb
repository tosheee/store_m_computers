<%= form_for(@admin_configuration_identifier) do |f| %>
  <% if @admin_configuration_identifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_configuration_identifier.errors.count, "error") %> prohibited this admin_configuration_identifier from being saved:</h2>

      <ul>
      <% @admin_configuration_identifier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identifier %><br>
    <%= f.text_area :identifier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
