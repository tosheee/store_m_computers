<%= form_for(@admin_api_translator) do |f| %>
  <% if @admin_api_translator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_api_translator.errors.count, "error") %> prohibited this admin_api_translator from being saved:</h2>

      <ul>
      <% @admin_api_translator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :worlds %><br>
    <%= f.text_area :worlds %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
